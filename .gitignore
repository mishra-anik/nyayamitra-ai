# Exclude node_modules anywhere in the repo (server/node_modules, client/node_modules, etc.)
node_modules/

# Exclude environment files everywhere
.env
.env.*
**/.next/

# Keep example environment files
!.env.example